метод Скрипт()
    /*
    //Задание 1
    пер ДниНедели = [
        "Понедельник", 
        "Вторник", 
        "Среда", 
        "Четверг", 
        "Пятница", 
        "Суббота", 
        "Воскресенье"
    ]
    пер ЧетвертыйДень = ДниНедели.Получить(3) 
    пер ШестойДень = ДниНедели[5]
    пер ТретийДень = ДниНедели[2]
    пер ПятыйДень = ДниНедели[4]
    Консоль.Записать(ДниНедели.Соединить(" "))
    
    //Задание 2
    пер СписокУчеников = ["Сергеев", "Дмитриев", "Захаров", "Максимов"]
    Консоль.Записать("Задание 2.\нИсходный список учеников: " + СписокУчеников.Соединить(" "))
    пер УченикДляУдаления = Консоль.СчитатьСтроку("Введите фамилию ученика для удаления из списка: ")
    если СписокУчеников.Найти(УченикДляУдаления) != Неопределено
        СписокУчеников.Удалить(УченикДляУдаления, Истина)
        Консоль.Записать("Ученик удален!")
        Консоль.Записать("Получившийся список: " + СписокУчеников.Соединить(" "))
    иначе
        Консоль.Записать("Ученика нет в списке!")  
    ;
    
    
    //Задание 41
    пер Годы: Массив<Число>
    пер ВисокосныеГоды: Массив<Число>
    для Год = 2000 по 2030
        Годы.Добавить(Год)
    ;
    для Год из Годы
        если Год % 4 == 0
            ВисокосныеГоды.Добавить(Год)
        ;
    ;
    Консоль.Записать("Список годов: $Годы")
    Консоль.Записать("Високосные годы: $ВисокосныеГоды")
    
    
    //для из только для чтения 
    //для по для изменения элементов
    пер Числа = [10, 8, 4, 15] 
    для Индекс = Числа.Граница() вниз по 0 //берем каждый эл с конца массива
        если Числа[Индекс] < 10 
            Числа.УдалитьПоИндексу(Индекс) 
        ; 
    ;
    
    
    пер НовыеЧисла = [10, 8, 4, 15, 9]
    пер ТекущийИндекс = НовыеЧисла.Граница()
    пока ТекущийИндекс >= 0
        если НовыеЧисла[ТекущийИндекс] >= 10
            НовыеЧисла.УдалитьПоИндексу(ТекущийИндекс)
        ;
        ТекущийИндекс -= 1
    ; 
    Консоль.Записать(НовыеЧисла)
    
    пер Отзыв = Консоль.СчитатьСтроку("Отзыв ( стоп - завершить): ")
    пока Отзыв.ВНижнийРегистр() != "стоп"
        Консоль.Записать("Отзыв принят!")
        Отзыв = Консоль.СчитатьСтроку("Отзыв ( стоп - завершить): ")
    ;
    Консоль.Записать("Конец.")*/
    
    //Многомерные массивы
    пер Таблица = [[1, 2], [3, 4], [5, 6], [7, 8]]
    Консоль.Записать(Таблица[0][0]) //обращение к первому элементу первого массива
    Таблица[1][1] = 9
    
    
    
;