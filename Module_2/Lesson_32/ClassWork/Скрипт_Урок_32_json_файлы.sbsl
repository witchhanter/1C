метод Скрипт()
    
    /*
    исп ПотокЗаписи = новый Файл("K:\\1С-ЭЛЕМЕНТ\\Ерофеев\\МойПервыйJson.json").ОткрытьПотокЗаписи()
    
    знч Запись = новый ЗаписьJson(ПотокЗаписи)
    
    Запись.ЗаписатьНачалоМассива()
    
        .ЗаписатьНачалоОбъекта()
            .Записать("Код", "000007")
            .Записать("Наименование", "ОАО Топаз")
            .ЗаписатьИмяСвойства("Телефоны")
            .ЗаписатьНачалоМассива()
                .Записать("+7(999)528-96-21")
                .Записать("+7(999)456-87-68")
            .ЗаписатьКонецМассива()
            .Записать("ОбъемПродаж", 5000000)
            .Записать("Поставщик", Истина)
        .ЗаписатьКонецОбъекта()
        
        .ЗаписатьНачалоОбъекта()
            .Записать("Код", "000008")
            .Записать("Наименование", "ОАО Пантера")
            .ЗаписатьИмяСвойства("Телефоны")
            .ЗаписатьНачалоМассива()
                .Записать("+7(999)345-73-84")
                .Записать("+7(999)423-23-01")
            .ЗаписатьКонецМассива()
            .Записать("ОбъемПродаж", 2500000)
            .Записать("Поставщик", Ложь)
        .ЗаписатьКонецОбъекта()
        
    .ЗаписатьКонецМассива()
    
    
    исп  ПотокЧтения  = новый  Файл("K:\\1С-ЭЛЕМЕНТ\\Ерофеев\\МойПервыйJson.json").ОткрытьПотокЧтения() 
    
    знч  Чтение  = новый  ЧтениеJson(ПотокЧтения)  
    
    пока  Чтение.Следующий()  
        выбор  Чтение.ВидУзла 
        когда  ИмяСвойства 
            Консоль.Записать("Имя  " + Чтение.Значение) 
        когда  Булево,  Строка,  Число,  Комментарий 
            Консоль.Записать("Значение  " + Чтение.Значение) 
        ; 
    ; 
   
          
    исп ПотокЗаписи = новый СтроковыйПотокЗаписи()
    
    знч Запись = новый ЗаписьJson(ПотокЗаписи)
    СформироватьJson(Запись)   
    пер  JsonСтрока  = ПотокЗаписи.ВСтроку() 
    Консоль.Записать(JsonСтрока)     
    
    
    
    
    
    
    исп ПотокЗаписи = новый Файл("K:\\1С-ЭЛЕМЕНТ\\Ерофеев\\МойПервыйJson.json").ОткрытьПотокЗаписи()
    знч Настройки = новый НастройкиЗаписиJson()
    Настройки.ПроверятьСтруктуру = Ложь
    // Настройки.ПереносСтрок = ПереносСтрокJson.Отсутствует
    Настройки.СимволыОтступа = Символы.ТАБ
    знч Запись = новый ЗаписьJson(ПотокЗаписи, Настройки)
    Запись.ЗаписатьНачалоМассива()
    
        .ЗаписатьНачалоОбъекта()
            .Записать("Код", "000007")
            .Записать("Наименование", "ОАО Топаз")
            .ЗаписатьИмяСвойства("Телефоны")
            .ЗаписатьНачалоМассива()
                .Записать("+7(999)528-96-21")
                .Записать("+7(999)456-87-68")
            .ЗаписатьКонецМассива()
            .Записать("ОбъемПродаж", 5000000)
            .Записать("Поставщик", Истина)
        .ЗаписатьКонецОбъекта()
        
        .ЗаписатьНачалоОбъекта()
            .Записать("Код", "000008")
            .Записать("Наименование", "ОАО Пантера")
            .ЗаписатьИмяСвойства("Телефоны")
            .ЗаписатьНачалоМассива()
                .Записать("+7(999)345-73-84")
                .Записать("+7(999)423-23-01")
            .ЗаписатьКонецМассива()
            .Записать("ОбъемПродаж", 2500000)
            .Записать("Поставщик", Ложь)
        .ЗаписатьКонецОбъекта()
        
    .ЗаписатьКонецМассива()
    
    
    //Десериализация (чтение)
    исп ПотокЧтения = новый Файл("K:\\1С-ЭЛЕМЕНТ\\Ерофеев\\МойПервыйJson.json").ОткрытьПотокЧтения()
    пер Партнеры = СериализацияJson.ПрочитатьМассив(ПотокЧтения) //нужно заранее знать структуру файла
    */
    
    //Читать можно с помощью сериализации или потоком
    //Задание 1
    исп ПотокЗаписи = новый Файл("K:\\1С-ЭЛЕМЕНТ\\Ерофеев\\card.json").ОткрытьПотокЗаписи()
    знч Запись = новый ЗаписьJson(ПотокЗаписи)
    Запись.ЗаписатьНачалоМассива()
        .ЗаписатьНачалоОбъекта()
            .Записать("Имя", "Игорь")
            .Записать("Возраст", 17)
            .Записать("Хобби", "Программирование")
        .ЗаписатьКонецОбъекта()
    .ЗаписатьКонецМассива()
    
    исп ПотокЧтения = новый Файл("K:\\1С-ЭЛЕМЕНТ\\Ерофеев\\card.json").ОткрытьПотокЧтения()
    пер Сотрудники = СериализацияJson.ПрочитатьМассив(ПотокЧтения)
    Консоль.Записать(Сотрудники)
    
    //Сериализация (запись строки)
    
;

метод СформироватьJson(Запись: ЗаписьJson)
    Запись.ЗаписатьНачалоОбъекта()
        .Записать("Код", "00000*")
        .Записать("Наименование", "ОАО Топаз")
        .ЗаписатьИмяСвойства("Телефоны")
        .ЗаписатьНачалоМассива()
            .Записать("+7(999)528-96-21")
            .Записать("+7(999)456-87-68")
        .ЗаписатьКонецМассива()
        .Записать("ОбъемПродаж", 5000000)
        .Записать("Поставщик", Истина)
    .ЗаписатьКонецОбъекта()
;