//Работа с файлами
метод Скрипт()
    //стр 187 - глава 6-ая можно самому
    /*
    //Файл.Существует
    пер ИсточникКаталог = новый Файл("K:\\1С-ЭЛЕМЕНТ")
    если не ИсточникКаталог.Существует()
        Консоль.Записать("Нет каталога-источника")
    ;
    //
    если не новый Файл("K:\\1С-ЭЛЕМЕНТ").Существует()
        Консоль.Записать("Нет каталога-источника")
    иначе
        Консоль.Записать("Существует")
    ;
    
    //Файлы.Найти
    попытка 
        пер СписокФайлов = Файлы.Найти("K:\\1С-ЭЛЕМЕНТ")
    поймать НетПути: ИсключениеФайловойСистемы
        Консоль.Записать("Нет каталога-источника ${НетПути.Описание}")
    ;
    
    //Файлы.Скопировать, ..
    // знч ПоискПоРегулярномуВыражению = новый НастройкиПоискаФайлов().ИмяСоответствует('.*\.txt?')
    // знч НастройкиКопирования = новый НастройкиКопированияФайлов().ПропускатьСуществующие(Истина)
    // для Файл из Файлы.Найти("K:\\1С-ЭЛЕМЕНТ\\Тест", ПоискПоРегулярномуВыражению)
    //     Файлы.Скопировать(Файл.Путь, "K:\\1С-ЭЛЕМЕНТ\\Результат4\\" + Файл.Имя, НастройкиКопирования)
    // ;
    
    //Файлы.Удалить
    //Файлы.Удалить("K:\\1С-ЭЛЕМЕНТ\\Результат4\\Выбор.txt", Истина)
    
    //Файл.СоздатьВременныйФайл //создаются только на время работы программы
    // знч Временный = Файлы.СоздатьВременныйФайл("_")
    // Консоль.Записать(Временный)
    // Консоль.СчитатьСтроку("Ожидание")
    
    
    
    //Чтение и запись текстовых файлов
    //.ОткрытьПотокЧтения
    // исп ПотокЧтения = новый Файл("K:\\1С-ЭЛЕМЕНТ\\_Материалы курса\\Стих.txt").ОткрытьПотокЧтения() //исп удаляется после работы с переменной, в которой храниться файл
    // пер Стих = ПотокЧтения.ПрочитатьКакСтроку()
    // Консоль.Записать(Стих)
    
    */
    
    исп ПотокЧтения1 = новый Файл("K:\\1С-ЭЛЕМЕНТ\\_Материалы курса\\Стих.txt").ОткрытьПотокЧтения() //исп удаляется после работы с переменной, в которой храниться файл
    знч Настройки = новый НастройкиЧтенияДанных()
    Настройки.Кодировка = "utf-8"//"ansi-1251"
    знч ЧтениеДанных = новый ЧтениеДанных(ПотокЧтения1, Настройки)
    пер НомерСтроки = 1
    пока не ЧтениеДанных.ЧтениеЗавершено()
        Консоль.Записать("${НомерСтроки}. ${ЧтениеДанных.ПрочитатьСтроку()}")
        НомерСтроки += 1
    ;
    //работу с файлами лучше делать в попытке-поймать Искл (стр200 уч)
    
    //.ОткрытьПотокЗаписи
    // исп ПотокЗаписи = новый Файл("K:\\1С-ЭЛЕМЕНТ\\_Материалы курса\\Стих4.txt").ОткрытьПотокЗаписи()
    // пер НовыйСтих = ПотокЗаписи.Записать(Стих)
    
    
;