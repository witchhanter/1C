//============
//===Урок 6===
//============
метод Скрипт()
    //№1
    // Консоль.Записать("Привет, " + Консоль.СчитатьСтроку("Введите Ваше имя: ") + "! Рад тебя видеть!")
    
    //№2
    // Консоль.Записать(
    // "Ваше ФИО: " 
    // + Консоль.СчитатьСтроку("Введите Вашу фамилию: ") + " " 
    // + Консоль.СчитатьСтроку("Введите Ваше Имя: ") + " " 
    // + Консоль.СчитатьСтроку("Введите Ваше Отчество: ")
    // )
    
    //№3
    // пер Предмет = "Геометрия"
    // пер Класс = "7"
    // пер НазваниеКурса = Предмет + ", " + Класс + " класс."
    // Консоль.Записать(НазваниеКурса)
    
    //№4
    // пер День = "15"
    // пер Месяц = "сентября"
    // пер Год = "2024"
    // пер ДатаВТекстовомФормате = День + " " + Месяц + " " + Год + " года"
    // Консоль.Записать(ДатаВТекстовомФормате) 
    
    //№5
    // пер Диск = "С:"
    // пер Папка = "Учеба"
    // пер ИмяФайла = "домашняя_работа"
    // пер Расширение = "txt"
    // пер ПолныйПуть = Диск + "\\" + Папка + "\\" + ИмяФайла + "." + Расширение
    // Консоль.Записать(ПолныйПуть)
    
    //№6
    // пер Протокол = "https:/"
    // пер Сайт = "/vkvideo.ru"
    // пер Команда = "/@neocso/clips?z="
    // пер КодВидео = "clip-72219450_456240125%2Fclub72219450"
    // пер Ссылка = Протокол + Сайт + Команда + КодВидео
    // Консоль.Записать(Ссылка)
    
    //№7
    // пер ВремяНачала = "10:30"
    // пер ВремяОкончания = "11:15"
    // пер Предмет = "Физика"
    // пер Кабинет = "305"
    // пер ЗаписьВРасписании = ВремяНачала + "-" + ВремяОкончания + " " + Предмет + " (каб. " + Кабинет + ")"
    // Консоль.Записать(ЗаписьВРасписании)
    
    //№8
    // пер Предмет = "Математика"
    // пер Оценка = "5"
    // пер Преподаватель = "Сергеева М.А."
    // пер Итог = "По " + Предмет + " итоговая оценка: " + Оценка + " (преподаватель: " + Преподаватель + ")"
    // Консоль.Записать(Итог)
    
    //№9
    // пер Логин = "school_student2026"
    // пер Домен = "yandex.ru"
    // пер Email = Логин + "@" + Домен
    // Консоль.Записать(Email)
    
    //========
    //ДатаВремя
    
    // Консоль.Записать(ДатаВремя.Сейчас())
    
    // пер НачалоЗанятий = ДатаВремя{2026-02-14 18:00:00}
    
    // пер КонецЗанятий = новый ДатаВремя(2026, 02, 14, 19, 45, 0)
    
    // Консоль.Записать(КонецЗанятий.Время)
    
    пер ВремяСейчас = ДатаВремя.Сейчас()
    
    пер НачалоСегодня = ВремяСейчас.НачалоДня()
    пер КонецСегодня = ВремяСейчас.КонецДня()
    
    // пер НачалоГода = ВремяСейчас.НачалоГода()
    // пер КонецГода =  ВремяСейчас.КонецГода()
    
    // пер НаКакоеТоВремяБольше = ДатаВремя.Сейчас() + 1д23м40с
    
    // пер МойПериод = 12д15ч17м
    // НаКакоеТоВремяБольше += МойПериод
    
    // пер ВремяСейчас = ДатаВремя.Сейчас()
    // пер ВремяЧерезЧас = ДатаВремя.Сейчас().ДобавитьЧасы(1).Время
    
    // пер НаКакоеТоВремяМеньше = ДатаВремя.Сейчас() - 1д
    
    пер ДлительностьСегодня = КонецСегодня - НачалоСегодня
    пер ДлительностьСегодняНаоборот = НачалоСегодня - КонецСегодня
    пер ЗанятиеСегодня = ДатаВремя{2026-02-14 18:00:00}
    пер НовоеЗанятие = ЗанятиеСегодня.СДнем(19)

;
