метод Скрипт()
    //13
    // пер Возраст = "16"
    // пер МойВозраст = "Мой возраст " + Возраст + " лет"
    //14
    // пер Улица = "Можайское ш."
    // пер НомерДома = "д. 37"
    // пер НомерКвартиры = "кв. 36"
    // пер Город = "г. Одинцово"
    // пер Область = "Московская обл."
    // пер Адрес = Улица + ", " + НомерДома + ", " + НомерКвартиры + ", " + Город + ", " + Область
    
    //Урок 5. Задачи
    //№1
    // пер КвоЧеловекВКлассе = 25
    // пер КвоКонфет = 201
    // пер ОстатокКонфет = КвоКонфет % КвоЧеловекВКлассе //остаток конфет
    // пер КонфетНаЧеловека = (КвоКонфет - ОстатокКонфет) / КвоЧеловекВКлассе 
    //№2
    пер Высота = 100 //см
    пер РостКаждыйДень = 20 //%
    пер КвоДней = 1 //дней
    пер ВысотаЧерезДень = Высота
    ВысотаЧерезДень *= 1.2 //высота через день
    ВысотаЧерезДень *= 1.2 //2
    ВысотаЧерезДень *= 1.2 //3
    ВысотаЧерезДень *= 1.2 //4
    ВысотаЧерезДень *= 1.2 //5
    ВысотаЧерезДень *= 1.2 //6
    ВысотаЧерезДень *= 1.2 //7
    // пер ВысотаЧерезНеделю = 
    
    //№3
    // пер КвоКоробок = 4
    // пер КвоДеталейВКоробке = 25
    // пер ВсегоДеталей = КвоКоробок * КвоДеталейВКоробке
    // ВсегоДеталей -= 3
    // ВсегоДеталей -= 5
    
    //№4
    // пер СтоимостьПиццы = 1200
    // пер СтоимостьДоставки = 100
    // пер КвоДрузей = 3
    // пер ДолженКаждый = (СтоимостьПиццы + СтоимостьДоставки) / КвоДрузей
    
    //№5
    // пер Энергия = 100
    // пер ЭнергияНаУровень = 15
    // пер КвоУровней = 3
    // Энергия -= ЭнергияНаУровень * КвоУровней
    // пер Энергетик = 50
    // Энергия += Энергетик
    
    //№6
    // пер Мука = 200
    // пер КвоЯиц = 2
    // пер ВесЯйца = 70
    // пер Сахар = 150
    // Мука *= 2.5
    // КвоЯиц *= 3
    // Сахар *= 2.5
    // пер ОбщийВес = (Мука + Сахар + КвоЯиц * ВесЯйца) / 1000
    
    //№7
    пер МояСкоростьПечати = 150
    пер ДлинаТекста = 5000
    пер МинутыНаПечать = ДлинаТекста / МояСкоростьПечати
    пер Минуты = МинутыНаПечать % 60
    пер Часы = (МинутыНаПечать - Минуты) / 60
    
    //№8
    пер Подписчики = 1000
    пер Прирост = 15
    Подписчики *= (Прирост / 100 + 1)
    пер Потеря = 5
    Подписчики *= (1 - Потеря / 100)
;